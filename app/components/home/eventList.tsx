import React from 'react';
import { EventCard} from './eventCard';

interface MyComponentProps {
  
}

export const EventList = (props: MyComponentProps) => {
  return (
    <div>
      {/* Component content goes here */}
    </div>
  );
}